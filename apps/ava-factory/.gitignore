# Factory local — see root .gitignore for canonical
__pycache__/
*.pyc
*.pt
*.bin
*.safetensors
checkpoints/
artifacts/
hf_model/
wandb/
logs/
.dolma/
.jupyter/
data/synthetic*/
*.log
.DS_Store
.env
venv/
.venv/

# Live telemetry — generated, not committed
STATUS.json
# glob, not exact names: the *_next_dryrun.json variant slipped past an exact
# pattern and got committed by a `git add -A`, breaking CI's telemetry guard
reports/dottie_live_status*.json
reports/ava_live_status.json
reports/dottie_telemetry.jsonl
reports/ava_telemetry.jsonl
reports/last_ecosystem_check.json
research-engine/results/results.tsv
research-engine/results/autoresearch_*.json

data/
runs/
ckpt/
export/
reports/*.html
manifest.jsonl
tasks/_status_snapshot.json
tasks/_tick_status.json

# cpu pilot binaries out, evidence in
!runs/
runs/*
!runs/cpu_pilot/
runs/cpu_pilot/*
!runs/cpu_pilot/MANIFEST.json
!runs/cpu_pilot/tokenizer/
!runs/cpu_pilot/reports/
!runs/cpu_pilot/reports/**

.venv-test/

# checkpoint_manager.py triple-write: run state is generated output, not source.
# Ungitignored it feeds the ast_pairs corpus-drift problem (612 untracked files
# mined as source, measured 2026-08-02) and grows with machine uptime.
bundles/ultra/runs/
dottie/pipeline/runs/
