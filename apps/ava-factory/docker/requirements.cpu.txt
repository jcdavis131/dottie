# Exact pins verified against huggingface.co from inside Docker on this host.
# datasets/pyarrow pairing is load-bearing: newer datasets with a mismatched
# pyarrow raises NoneType.ArrowInvalid on the first streamed parquet batch.
datasets==2.20.0
pyarrow==16.1.0
fsspec[http]==2024.5.0
huggingface-hub==0.23.4

# tokenizer (trained locally; no hub download)
tokenizers==0.19.1

# curation
datasketch==1.6.5          # MinHash LSH near-dedup
zstandard==0.22.0          # raw shard compression
regex==2024.5.15           # PII scrub, unicode-aware
numpy==1.26.4

# plumbing
PyYAML==6.0.1
tqdm==4.66.4
pytest==8.2.2
