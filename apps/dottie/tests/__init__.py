# Solo personal project, no connection to employer, built with public/free-tier only
# Regular package (not a namespace package): tests import `tests.conftest` explicitly, and a
# namespace `tests` loses to any stray regular `tests` package in site-packages.
