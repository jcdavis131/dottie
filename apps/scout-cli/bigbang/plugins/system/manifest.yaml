name: system
version: 0.4.0
description: System health, audit, policy, scaffold
capabilities:
  network:
    enabled: true
    domains: ["localhost"]
  filesystem:
    write: true
    paths: ["~/workspace/bigbang-cli/"]
