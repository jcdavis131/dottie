# --- Python / General ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.ipynb_checkpoints
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.env
*.log
*.tmp

# --- IDE ---
.vscode/
.idea/
*.swp
.cursor/

# --- ML Artifacts (never commit binaries) ---
*.pt
*.bin
*.safetensors
*.onnx
checkpoints/
ckpt/
hf_model/
wandb/
mlruns/
data/
runs/
export/
*.arrow
*.parquet

# --- Dottie Factory Generated (telemetry / live status) ---
# These are generated by crons every 4h / hourly — not source
apps/ava-factory/STATUS.json
apps/ava-factory/reports/dottie_live_status.json
apps/ava-factory/reports/ava_live_status.json
apps/ava-factory/reports/dottie_telemetry.jsonl
apps/ava-factory/reports/ava_telemetry.jsonl
apps/ava-factory/reports/bench_pipeline.json
apps/ava-factory/reports/last_ecosystem_check.json
apps/ava-factory/reports/t12_2_nano_quick.json

# Research engine live results (changes every hour)
apps/ava-factory/research-engine/results/results.tsv
apps/ava-factory/research-engine/results/autoresearch_*.json
apps/ava-factory/research-engine/results/autoresearch_*.jsonl

# Packed shards / manifests (generated)
apps/ava-factory/data/
apps/ava-factory/packed/
apps/ava-factory/manifest_*.jsonl
apps/ava-factory/upload_manifest_*.json

# CPU pilot binaries (keep evidence json only)
apps/ava-factory/runs/cpu_pilot/*.pt
apps/ava-factory/runs/cpu_pilot/*.bin

# --- Scout / RTX local ---
autoresearch-rtx-custom/
apps/scout-rtx/runs/
apps/scout-rtx/results/
apps/scout-cli/autoresearch-rtx-custom/

# --- Web artifacts / Vercel ---
.vercel/
dist/
.build/
node_modules/
.ts-spaces/
ts-spaces/*/dist/
ts-spaces/*/app.db
ts-spaces/*/app.db-journal

# --- Life Admin Brain local DB (template is public, data is private) ---
# Keep app.db schema? ignore runtime DB
life-admin-brain/app.db
your_files/

# --- Secrets ---
**/secrets.json
**/.env.local
**/credentials.json

# --- Experiments scratch (keep curated ones, ignore new until committed manually) ---
# We keep experiments/ committed explicitly, but results/ json is ignored above

# --- Dolma / Data tooling ---
.dolma/
.jupyter/
.venv-test/

# Cygwin/MSYS crash dumps — an artifact of a tool crashing, never source
*.stackdump

# Night shift lane 2: Dottie pipeline checkpoints should not be committed — triple-write canonical
apps/ava-factory/dottie/pipeline/runs/
apps/ava-factory/bundles/ultra/runs/
bundles/ultra/runs/


# --- Dottie SOTA sessions / harness (prime-agent edition, local-only) ---
.dottie/
.scout/
workspace/.scout/
.scout/missions/
.dottie/harness/
.dottie/inbox/
.dottie/registry.json
.dottie/goals.jsonl
tmp_prime_*.json


# next build
.next/
apps/arxiviq/.next/
out/

